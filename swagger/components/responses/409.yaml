description: Conflict - Resource already exists
content:
  application/json:
    schema:
      $ref: '../schemas/errors.yaml#/ConflictError'
    example:
      status: 409
      message: "User with this email already exists"
